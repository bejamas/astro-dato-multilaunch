---
const { label, slug, variant = "primary" } = Astro.props;
---

<a 
  href={slug} 
  class={`px-4 py-2 text-xs whitespace-nowrap uppercase transition-all duration-200 
    ${variant === "primary" ? "bg-primary text-primary-foreground" : "bg-primary-foreground text-primary border border-border"}`}>
  {label}
</a>

